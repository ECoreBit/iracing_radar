# SimHub package

The current implementation is documented in the repository [README](../README.md).

Contents:

- `IRacingRadarPlugin`: C# source and synthetic test.
- `Overlay`: installable Dash Studio overlay and packaged image resources.

Use `scripts/build-plugin.ps1`, `scripts/test.ps1`, and `scripts/install.ps1` from the repository root.